{
 "cells": [
  {
   "cell_type": "markdown",
   "id": "c08de51c",
   "metadata": {},
   "source": [
    "# 高级手写题（基础与语法相关）\n",
    "\n",
    "请在下方按顺序完成每道题。每题写在一个单独的 JavaScript 代码单元内，完成后用 `console.log` 添加 1~2 个测试用例。\n",
    "\n",
    "题目列表：\n",
    "\n",
    "1. 实现 `myInstanceof(obj, Constructor)`\n",
    "2. 实现 `myNew(Ctor, ...args)`\n",
    "3. 实现节流 `throttle(fn, wait)`（前沿触发 / 尾触发 / 可配置版）\n",
    "4. 实现防抖 `debounce(fn, wait)`（立即执行 / 非立即执行）\n",
    "5. 实现一个 `once(fn)`，函数只执行一次\n",
    "6. 实现 `compose(...fns)` 和 `pipe(...fns)`\n",
    "7. 实现一个简单的 `EventEmitter`（`on / off / once / emit`）\n",
    "8. 实现一个 `sleep(ms)`，支持 `async/await`\n",
    "9. 手写 `call / apply / bind`\n",
    "10. 实现对象的浅拷贝和基础版深拷贝（先不考虑循环引用）\n",
    "\n",
    "下面为每道题预先创建了空的 JavaScript 代码单元模板，方便你逐题实现。完成后告诉我，我可以帮你运行测试或给出参考解答。"
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "id": "0e9c2376",
   "metadata": {
    "vscode": {
     "languageId": "javascript"
    }
   },
   "outputs": [],
   "source": [
    "// 题1: 实现 myInstanceof(obj, Constructor)\n",
    "// 在此实现并测试：\n",
    "// function myInstanceof(obj, Constructor) { /* ... */ }\n",
    "// console.log(myInstanceof([], Array)); // true"
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "id": "7e3e9795",
   "metadata": {
    "vscode": {
     "languageId": "javascript"
    }
   },
   "outputs": [],
   "source": [
    "// 题2: 实现 myNew(Ctor, ...args)\n",
    "// 在此实现并测试：\n",
    "// function myNew(Ctor, ...args) { /* ... */ }\n",
    "// const o = myNew(Person, 'Alice');"
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "id": "4990fcf2",
   "metadata": {
    "vscode": {
     "languageId": "javascript"
    }
   },
   "outputs": [],
   "source": [
    "// 题3: 实现 throttle(fn, wait)（可实现前沿/尾触发的可配置版）\n",
    "// 在此实现并测试：\n",
    "// const t = throttle(fn, 200);"
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "id": "d7f3d237",
   "metadata": {
    "vscode": {
     "languageId": "javascript"
    }
   },
   "outputs": [],
   "source": [
    "// 题4: 实现 debounce(fn, wait)（立即执行 / 非立即执行）\n",
    "// 在此实现并测试：\n",
    "// const d = debounce(fn, 300);"
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "id": "6928526a",
   "metadata": {
    "vscode": {
     "languageId": "javascript"
    }
   },
   "outputs": [],
   "source": [
    "// 题5: 实现 once(fn)（函数只执行一次）\n",
    "// 在此实现并测试：\n",
    "// const o = once(fn); o(); o(); // 仅第一次生效"
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "id": "2a5882a5",
   "metadata": {
    "vscode": {
     "languageId": "javascript"
    }
   },
   "outputs": [],
   "source": [
    "// 题6: 实现 compose(...fns) 和 pipe(...fns)\n",
    "// 在此实现并测试：\n",
    "// const f = compose(f1, f2); f(x);"
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "id": "3edd38e4",
   "metadata": {
    "vscode": {
     "languageId": "javascript"
    }
   },
   "outputs": [],
   "source": [
    "// 题7: 实现 EventEmitter（on/off/once/emit）\n",
    "// 在此实现并测试：\n",
    "// const e = new EventEmitter(); e.on('a',cb); e.emit('a');"
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "id": "2d2a69a6",
   "metadata": {
    "vscode": {
     "languageId": "javascript"
    }
   },
   "outputs": [],
   "source": [
    "// 题8: 实现 sleep(ms)，支持 async/await\n",
    "// 在此实现并测试：\n",
    "// await sleep(1000); console.log('done');"
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "id": "08bd882b",
   "metadata": {
    "vscode": {
     "languageId": "javascript"
    }
   },
   "outputs": [],
   "source": [
    "// 题9: 手写 call / apply / bind\n",
    "// 在此实现并测试：\n",
    "// Function.prototype.myCall = function(ctx, ...args) { ... }"
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "id": "57ea5837",
   "metadata": {
    "vscode": {
     "languageId": "javascript"
    }
   },
   "outputs": [],
   "source": [
    "// 题10: 对象浅拷贝与基础深拷贝（先不处理循环引用）\n",
    "// 在此实现并测试：\n",
    "// function shallowClone(obj) { ... }\n",
    "// function deepClone(obj) { ... }"
   ]
  }
 ],
 "metadata": {
  "language_info": {
   "name": "python"
  }
 },
 "nbformat": 4,
 "nbformat_minor": 5
}
